// INB drawing-template symbol palette categories.
//
// PARKED: the imported categories (symbolen / bouwkundig / markers) are
// temporarily disabled per user request — the underlying generated data can
// be re-added from version control when this gets picked up again.
export const INB_CATEGORIES = [];
