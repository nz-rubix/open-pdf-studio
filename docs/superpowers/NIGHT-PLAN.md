# Autonomous night — MV-03 speed + GitHub issues

**READ THIS FIRST each wake-up.** Branch: `night/perf-mv03` (off main; keep main clean). Work MYSELF via MCP (subagents keep dying). systematic-debugging: prove root cause by measurement BEFORE any fix; ONE change/iteration → MCP-test → measure → KEEP or REVERT; commit each verified win on the branch.

## Goal
1. Make MV-03 (huge A0 sheet, one giant FlateDecode image) **visible-paint + zoom enormously faster**.
2. THEN: solve testable GitHub issues, verify each via MCP.

## Current state (checkpoint 0870f573)
- `renderPage TOTAL = 32ms` — the detached-overlay win is SOLID (was 6.9s). Keep it.
- `FIRST VISIBLE PAINT (bitmap) = 18402ms` cold on the DEBUG rig — **#1 problem**.
- Detached overlays take ~12.8s (still PDF.js `getAnnotations` for links/forms — NOT migrated).
- Dead-subagent's UNVERIFIED edits in `bitmap-orchestrator.js`, `pdf-viewport.js`, `worker_pool/mod.rs` may be a regression.
- Phase-1 text path partially in: Rust `extract_page_text_pdfium` + `text-layer.js` routed. Tekst.pdf renders fine, 0 errors.

## Iteration plan (do in order; measure each)
1. **Is 18.4s a dead-subagent regression?** `git checkout 98174045 -- open-pdf-studio/js/pdf/bitmap-orchestrator.js open-pdf-studio/js/pdf/pdf-viewport.js open-pdf-studio/src-tauri/src/worker_pool/mod.rs` (98174045 = pre-dead-subagent), rebuild, cold-measure FIRST VISIBLE PAINT. Drops a lot → keep reverted + commit. Same → restore those files (`git checkout 0870f573 -- ...`) and go to 2.
2. **Why bitmap paints at 18.4s (after overlays at 12.8s)?** Hypothesis: detached overlays starve the bitmap. Test: in `renderer.js` defer the detached `_buildInteractionOverlays` until AFTER the first visible paint (wait for `viewport.currentBitmap`), then measure.
3. **Kill the 7.4s `getAnnotations`:** add Rust `extract_page_annotations` (pdfium-render `page.annotations()`), route the link/form layers + loader annotation import. Verify links + annotations still work.
4. **Zoom-freeze:** cold-open MV-03, `app_set_zoom {zoom}` / `app_wheel_zoom {x,y,deltaY}`, read console for `[JANK] Main thread blocked Nms`. Root-cause the main-thread block (likely RGBA→ImageData→createImageBitmap on main thread in `bitmap-orchestrator.js` region path). Fix: off-thread / debounce / reuse capped bitmap during gesture. Measure jank before/after.
5. After MV-03 solid (visible <~1s if possible on release-class; or prove the floor) + committed → **GitHub issues** (testable code ones): #241 gray-out buttons w/o PDF, #238 polyline color, #240/#196 untranslated menus, #221 render-bleed prev PDF, #232 thumbnail missing text, #197 nav button blue when open, #230 tab overlap, #204 objects not staying after save. One per iteration, fix + MCP-verify + commit.

## Don't fight the floor
PDFium FlateDecoding the multi-MB A0 image is irreducible at real resolution. Only remove WASTE (contention, redundant/duplicate renders, main-thread bitmap work, un-debounced zoom). If a delay is the decode floor, document it — don't thrash.

## Rig + build recipe
- Restart rig (JS-only changes need only this): stop the `open-pdf-studio.exe` with `--mcp-server` in CommandLine, then PowerShell `$env:OPDS_DETACHED="1"; Start-Process -FilePath "C:/Users/rickd/AppData/Local/Temp/opds-build-166/debug/open-pdf-studio.exe" -ArgumentList "--mcp-server"`; poll `http://127.0.0.1:9223/mcp` (POST tools/list) until 200, wait ~3s.
- Rust rebuild (steps 1,3): stop rig first, then `$env:CARGO_TARGET_DIR="C:/Users/rickd/AppData/Local/Temp/opds-build-166"; cargo build --manifest-path "C:/Users/rickd/Documents/GitHub/open-pdf-studio/open-pdf-studio/src-tauri/Cargo.toml"` (few min; non-zero exit ONLY from updater-signing OK if .exe built).
- MCP: `Invoke-RestMethod -Uri http://127.0.0.1:9223/mcp -Method POST -Body (@{jsonrpc='2.0';id=1;method='tools/call';params=@{name=$N;arguments=$A}}|ConvertTo-Json -Depth 8 -Compress) -ContentType 'application/json'` → `.result.content[0].text`. Tools: app_open_pdf{path}, app_get_recent_console, app_screenshot_view (canvas png_base64 — confirm not blank), app_set_zoom{zoom}, app_wheel_zoom{x,y,deltaY}, app_new_blank_pdf{widthPt,heightPt}, app_create_annotation{type,page,props}, app_set_tool{tool}. For COLD measure ALWAYS restart rig first.
- Test files: MV-03 = `C:\Users\rickd\Documents\GitHub\verification-files\PDF-bestanden\MV-03_Mechanische ventilatie, 3e verdieping ontwerp ACH van 1,5 naar 2,0.pdf`; normal = `C:\Users\rickd\Documents\GitHub\open-pdf-studio\test pdf-bestanden\Originele bestanden\Tekst.pdf`; vector = `...\Technische tekening.pdf`.

## Commit rules (autonomous, no asking — user authorized)
Commit each verified win on `night/perf-mv03`. NO Claude/Anthropic in messages. NO competitor-software names / chat-history in repo. `git add -u` (no build artifacts / no docs/superpowers). Update the "Progress log" below each iteration.

## Progress log
- [start] Checkpoint 0870f573. renderPage 32ms; FIRST VISIBLE PAINT 18.4s (debug rig). Next: iteration 1 (revert dead-subagent 3 files, measure).

- [iter1 DONE] Reverted dead-subagent worker/orchestrator/viewport files. Visible paint 19.5s (was 18.4s WITH them) = no regression from them, no benefit either; discarded unverified guesses. ROOT CAUSE PROVEN: active-page bitmap is bucket 0.25 (843x596, 0.5MP, fast to render) but paints at 19.5s = QUEUED behind thumbnail + text-extraction renders on the single PDFium worker. Contention, not render cost. renderPage 32ms, overlays detached 13.7s.
- [iter2 NEXT] Prioritize the active-page bitmap over thumbnails. Investigate renderer.js resumeThumbnails()/pause-window + how generateThumbnails (left-panel.js) queues vs the active bitmap (bitmap-orchestrator ensureBitmapForCurrentView). Hypothesis: thumbnails (and/or extract_page_text_pdfium) saturate the single PDFium worker before the active page bitmap. Fix: pause/defer thumbnail generation until the active-page bitmap has painted; measure visible paint before/after.

- [iter2 DONE] Overlay-gate (defer overlays until bitmap paints) REFUTED — bitmap still ~20s when overlays gated AFTER it. Reverted. Found+removed a rogue iteration-1 PDF.js render PROBE in loader.js (full page rasterize, ~41s, every cold open) — committed. Thumbnail-disable test: overlays 14.7s->9.1s (in-process contention real) but the noisy 35s bitmap run was zombie pollution, not signal.
- [ENV FIX — important] Rig restarts ORPHAN child processes: force-killing the parent leaves pdfium-worker.exe + msedgewebview2 children alive. Tonight accumulated 4 pdfium-workers + 40+ webviews (one 5GB) => measurement variance (18s..35s). Cleaned. RESTART MUST KILL THE TREE: Stop pdfium-worker.exe AND open-pdf-studio.exe before each restart. Clean baseline reproduces at ~19s (the variance was zombies, the ~19s is real).
- [iter3 DONE — GROUND TRUTH] Instrumented pdfium-worker/src/render.rs; captured via main-process stderr redirect (worker stderr = Stdio::inherit). MV-03 @ scale 0.25 -> 843x596 decomposes as:
    * pdfium render_with_config = 10.0s  (read=4ms parse=0ms rgba=0ms) = the decode+raster floor.
    * WorkerPool::render round-trip = 13.9s  (~3.9s IPC/SHM/scheduling on top).
    * JS invoke('render_pdf_page') = 19.2s  (~5.3s more: in-process render_thumbnail=19s HOGS the main thread, delaying delivery of the bitmap that was READY at 13.9s).
    * render_thumbnail (in-process, 140px out) = 19s = redundant FULL re-decode of the same image, run concurrently -> contention inflates everything.
    * render.rs RE-READS + RE-PARSES the file every call (cheap: 4+0ms) but has NO bitmap/decode cache. The path-keyed caches live ONLY in the in-process fallback (pdfium_renderer.rs), which the pool path BYPASSES.
  ROOT CAUSE of the 19s first paint = ~10s pdfium decode floor, DOUBLED by (a) a redundant concurrent thumbnail re-decode that ALSO blocks bitmap delivery, (b) ~4s pool IPC. Attackable waste = thumbnail re-decode + contention + IPC. The 10s decode is the floor unless the image codec is reducible.
- [iter4 NEXT] Kill the redundant thumbnail decode: derive page-1 thumbnail by DOWNSCALING the already-rendered page bitmap (843x596 -> 140px) instead of a fresh 19s pdfium render; AND/OR defer generateThumbnails until AFTER the active bitmap has painted (loader.js call site). Expected: bitmap delivered ~13-14s (from 19s) + thumbnail instant + less contention. Measure cold MV-03 before/after (clean env, TREE-KILL restart, stderr redirect to read [worker PERF]).
    iter5: worker-side bitmap cache keyed (path,page,scale,rot) -> fixes zoom/re-render + zoom-freeze (re-decode per bucket today).
    iter6: shave the ~4s pool IPC/SHM overhead.
    iter7: is the 10s pdfium decode codec-reducible (is the image JPXDecode/JPEG2000?) or accept floor + add a placeholder/spinner UX.
- UNCOMMITTED measurement aids still in tree: open-pdf-studio/js/pdf/page-bitmap-cache.js ([PERF] bitmap invoke START/RETURN) + pdfium-worker/src/render.rs ([worker PERF] eprintln). Remove or env-gate before committing worker changes. Rig stderr capture file: C:/Users/rickd/AppData/Local/Temp/opds-rig-stderr.log (start rig with -RedirectStandardError).

- [iter3 addendum — the ~5s "mystery" explained] render_thumbnail is a SYNC #[tauri::command] fn (lib.rs:1656) => runs ON THE MAIN THREAD. Its 19s (render_thumbnail_to_json, lib.rs:1698) blocks the Tauri event loop, so the async render_pdf_page bitmap result (READY at 13.9s in the pool) cannot be delivered to JS until the main thread frees at ~19s. That IS the 13.9s->19.2s gap.
  Why not just make it async+spawn_blocking: caches are plain Mutex (PdfBytesCache lib.rs:41, ThumbnailCache:54) and the in-process handle is pdfium_renderer::PdfiumDocCache(Mutex<HashMap<String, Arc<PdfiumDocumentHandle>>>) — pdfium is NOT Send in-process (that's WHY render_pdf_page uses a multi-PROCESS worker pool). So moving PdfiumDocumentHandle into spawn_blocking won't compile. Routing thumbnails through the worker pool is possible but a real refactor (pool returns RGBA, thumb needs encoded data-url).
  CHOSEN iter4 (pure JS, no rebuild, no Send issue): derive the ACTIVE page's thumbnail by DOWNSCALING the page bitmap that bitmap-orchestrator already produced (843x596 -> 140px via canvas drawImage), push to the thumbnail store (setThumbnailImage), and SKIP render_thumbnail for that page in left-panel.js generateThumbnails. Eliminates the 19s re-decode + the main-thread block in one move. For MV-03 (1 page) that fully removes the thumbnail cost. Multi-page: other pages still use render_thumbnail (background processor) — acceptable; revisit if a huge multi-page sheet stalls.
  Verify: cold MV-03 (clean env, TREE-KILL restart, stderr redirect). Expect JS bitmap paint ~13-14s (from 19s) AND no [PERF-RUST] render_thumbnail for page 0. Then iter5 worker bitmap cache (zoom), iter6 pool IPC, iter7 decode-floor/codec.

- [iter4 DONE — REFRAMING] Implemented thumbnail-from-bitmap (Part A, COMMITTED cb58b052) + defer-generateThumbnails-until-bitmap (Part B). Measured cold MV-03 with stderr capture. RESULT + new root cause:
    * Worker render dropped 10.0s -> 6.5s and NO [PERF-RUST] render_thumbnail fired => the redundant thumbnail decode IS eliminated (Rust-VERIFIED). Good.
    * BUT JS visible paint went 19s -> 47s. Full console shows: Rust pool result READY at ~10s, main thread IDLE 10s..29s, yet JS `await invoke('render_pdf_page')` doesn't resolve until +47s.
    * ROOT CAUSE (new, dominant): Tauri/WebView2 IPC DELIVERY STARVATION. The 2 MB bitmap Response sits undelivered until the webview event loop is pumped (2 MB over 37s ~= 54 KB/s = a DEBUG-build pathology; release IPC is ~1000x faster). The sync render_thumbnail (19s) was ACCIDENTALLY PUMPING that delivery, so baseline "painted at 19s". Removing it (Part B) exposed the latent IPC delay -> 47s.
  IMPLICATIONS:
    1. The DEBUG RIG CANNOT MEASURE release visible-paint — it is IPC-delivery-dominated, not pdfium-dominated. ALL prior "18-19s paint" numbers were largely this artifact. Do NOT trust debug visible-paint for paint optimizations.
    2. Part B (defer thumbnails) is release-correct (on fast IPC the bitmap paints ~10s, deferral waits, thumb derives) but REGRESSES the debug rig (removes the accidental pump) and is UNVERIFIABLE here. REVERTED Part B (loader.js) per verification-before-completion. Kept Part A (neutral on first load, helps revisits).
    3. The redundant thumbnail decode (render_thumbnail ignores skip_images, re-decodes the A0 image ~19s, sync => blocks main thread) is a CONFIRMED release bug. Real release fix = Part B (defer) OR make render_thumbnail async/pool-routed. Validate on a RELEASE build.
- [DECISION] Debug rig is EXHAUSTED for MV-03 paint optimization (IPC-delivery noise masks everything). Real release levers, all needing a RELEASE-build rig to validate: (a) Part B defer-thumbnail, (b) worker-side bitmap cache keyed (path,page,scale,rot) for zoom-freeze + re-render, (c) probe the image codec (JPXDecode?) for the ~10s decode floor. PIVOTING the autonomous loop to the user's other explicit request: GitHub issues (verifiable via MCP, not paint/IPC-dependent).
- [iter5 NEXT = GitHub issues] gh issue list on OpenAEC-Foundation/open-pdf-studio; pick code-testable ones (#241 gray-out buttons w/o PDF, #238 polyline color, #240/#196 untranslated menus, #221 render-bleed, #232 thumbnail missing text, #197 nav btn blue, #230 tab overlap, #204 objects gone after save). One per iteration: reproduce via MCP, fix, MCP-verify, commit on night/perf-mv03. Rig restart MUST tree-kill (pdfium-worker + open-pdf-studio + msedgewebview2 matching open-pdf-studio).

- [iter5 DONE — GitHub issue TRIAGE] Checked the code-testable bug candidates against CURRENT code. ALL 4 are ALREADY FIXED (reporters were on old versions; fixes shipped by v1.69.0):
    * #238 Polyline color — FIXED: 'polyline' is in _STROKE_COLOR_DRIVEN (propertiesStore.js:20), color->strokeColor mirrored. Commit b63e199b, in v1.69.0.
    * #221 Render bleed on doc switch — FIXED: tabs.js:98-119 deactivates viewport singleton + clearRect on #pdf-canvas/#annotation-canvas + removes stale textLayer. Commit 67fa70c7, in v1.61.0..v1.69.0.
    * #197 Nav button blue when open — FIXED: ViewTab.jsx:57 nav button has active={!leftPanelCollapsed()} (and properties btn active at :60).
    * #241 Gray out buttons w/o PDF — FIXED: every CommentTab.jsx button has disabled={noPdf() || isPdfAReadOnly()} (lines 44-157). (Possible residual: confirm RibbonButton.jsx applies a visibly-grayed style when disabled — the v1.67 screenshot looked enabled; low priority.)
  CONCLUSION: the open-issue backlog is LARGELY STALE. The user fixed these but never closed the issues. NOTE: closing/commenting GitHub issues is a public action -> NOT done autonomously; report to user to close.
  USEFUL TECHNIQUE: issue screenshots are viewable — `curl -sL <user-attachments-url> -o tmp.png` then Read the png. Used for #241.
- [iter6 NEXT] (a) Translation issues are the most plausibly-still-open for a Dutch user: #196 (Feedback menus untranslated -> check FeedbackDialog.jsx for hardcoded English; HelpTab.jsx already uses t()) and #240 (What's New NL==EN -> WhatsNewDialog.jsx/whatsNewStore.js; likely a hardcoded/English-only changelog). Fix #196 if it's hardcoded labels (safe: short standard terms -> wrap in t() + add nl keys). #240 changelog body is risky to auto-translate; only fix the dialog chrome if that's the gap. (b) Systematic triage: classify remaining open issues fixed/platform(Linux,macOS — unverifiable on Windows)/feature-request/genuine. (c) BIG: MV-03 needs a RELEASE-build rig to validate the thumbnail-defer + measure true paint (debug rig = IPC-noise).

- [iter6 DONE — GitHub issues continued]
    * #196 (menus untranslated in Dutch) — GENUINE & BROAD. Root cause: 193 EN keys were missing from the NL locale -> i18next falls back to English. Confirmed via en-vs-nl key diff (script: AppData/Local/Temp/locale-diff.mjs + merge-nl.mjs). FIXED so far: title-bar sendFeedback (commit b9a34fc6) + common.json(20)+context.json(8) = 28 keys (commit 11305985). REMAINING 165: appMenu 17, dialogs 40, preferences 72, properties 10, ribbon 24, statusbar 2. The FeedbackDialog itself was already translated (nl/dialogs.json feedback.*); the screenshot was a pre-fix version.
    * #240 (What's New NL==EN) — NOT a code bug. WhatsNewDialog + parseBilingualBody (release-notes.js) correctly split a release body on markers `## 🇳🇱 Nederlands` / `## 🇬🇧 English` and pick by language. The v1.69.0 GitHub release body has NO such markers (generic English template) -> both languages show the same English. FIX = a RELEASE-PROCESS change: write release notes with those two section markers (and real per-version content). No autonomous code/edit (editing GitHub releases is a public action). Report to user.
    * Already-fixed (recommend user closes): #238, #221, #197, #241 (see iter5).
- [iter7 NEXT] Continue NL i18n completion in batches with merge-nl.mjs (structure-preserving, clean diff). Order by visibility: ribbon.json(24) + properties.json(10) + statusbar.json(2) next; then appMenu(17), dialogs(40), preferences(72). For each: dump EN values for the missing keys, translate to app-consistent Dutch (PRESERVE {{interpolation}} + \n), merge, `node` JSON-validate, re-run locale-diff to confirm 0 missing, commit per batch. NOTE: native-speaker user should review Dutch phrasing (it's strictly better than the English fallback though). Then resume MV-03 only with a RELEASE build (debug rig = IPC noise).

- [iter7 DONE — #196 FULLY RESOLVED] Completed the Dutch locale. 194 keys translated across 6 commits (b9a34fc6, 11305985, f1285290, fbe2c7a7, 34ec67b8, c09d4926): title-bar sendFeedback + common/context(28) + ribbon/properties/statusbar(36) + appMenu(17) + dialogs(40) + preferences(72). FINAL en-vs-nl diff = 0 missing. Matched the app's EXISTING Dutch vocabulary (snap=Uitlijnen, Lijnkleur, Lijnbreedte, Randstijl, Dekking, "Schaal instellen"). All JSON validated + interpolation ({{name}}/{{count}}/{{error}}) + \n preserved. Merge tooling: AppData/Local/Temp/{locale-diff,merge-nl*}.mjs (structure-preserving, additions-only diffs). NOTE for user: a native-speaker review of phrasing is nice-to-have (esp. preferences CAD terms like Poolvolging/Uitlijnstraal) but every key is now Dutch instead of the English fallback.
- [iter8 NEXT] The code-testable + translation issues are DONE. Remaining open issues are mostly: platform-specific (Linux/macOS launch/render — UNVERIFIABLE on this Windows rig: #249 #245 #244 #242 #235 #218 #217 #213 #208 #183 #182), feature requests (#239 #237 #236 #234 #233 #226 #225 #212 #209 #206 #205 #201 #200 #202 #157 #83 #58 #57 #56 #51), or hard rendering/font bugs (#216 TIF, #215/#158 fonts/text, #204 objects-after-save[old], #61 Hebrew). Do ONE systematic-triage pass: classify each remaining open issue (already-fixed / platform / feature-request / genuine-open) into a report for the user, checking current code where quick. Then the loop is near-exhausted for autonomous verifiable work → wind DOWN (longer interval / occasional CI+issue check). MV-03 remains blocked on a RELEASE-build rig.
